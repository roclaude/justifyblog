// Ajax for Signature form

( function($) {
	$( document ).ready( function($) {
/*
		$( '#justifyblog-publish' ).submit( function(e) {
			var self = $( this );
			e.preventDefault();
			
			$data_serialized = $("#justifyblog-publish").serialize();
						
			$.ajax({
				type: 'POST',
				url: addideaAjax.ajaxurl,
				data: {
					'action': 'justifyblog_add_idea_submit',
					'data' : $data_serialized,
				},
				success: function(data) {				
					$( '#main' ).html( data );
				},
				error: function(errorThrown){
					console.log(errorThrown);
				}
			});			
		});
*/
	});
} )( jQuery );